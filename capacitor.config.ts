import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'se.fabianandersson.ballknowledge',
  appName: 'Ball Knowledge',
  webDir: 'dist',
};

export default config;
